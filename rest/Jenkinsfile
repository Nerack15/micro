pipeline {
    agent any
    tools {
        gradle 'Gradle 7.5.1' 
    }
    stages {
        stage('Build') {
            steps {
                //sh 'gradle init'
                //sh 'gradle task'
                //sh './gradlew run'
                //sh 'gradle init'
                //sh 'gradle wrapper'
                //sh './gradlew build'
                //sh 'gradle assemble'
                sh 'Build'
            }
        }
        stage('test') {
            steps {
                //sh 'gradle docker'
                echo 'test succesfull'
            }
        }
        stage('Build Docker Image') {
            steps {
                //sh 'gradle docker'
                echo 'succesfull'
            }
        }
        stage('Push Docker Image') {
            steps {
                //sh 'gradle dockerPush'
                echo 'succesfull'
            }
        }
    }
}

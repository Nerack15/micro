pipeline {
    agent any
    tools {
        gradle 'Gradle 7.5.1' 
    }
    stages {
        stage('Build') {
            steps {
                sh 'build'
                //sh 'gradle task'
                //sh 'gradle init'
                //sh 'gradle wrapper'
                //sh './gradlew build'
                //sh 'gradle assemble'
                echo 'Build melo'
            }
        }
        stage('test') {
            steps {
                //sh 'gradle docker'
                echo 'test succesfull'
            }
        }
        stage('Build Docker Image') {
            steps {
                //sh 'gradle docker'
                echo 'succesfull'
            }
        }
        stage('Push Docker Image') {
            steps {
                //sh 'gradle dockerPush'
                echo 'succesfull'
            }
        }
    }
}
